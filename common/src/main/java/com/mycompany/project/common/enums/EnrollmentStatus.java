package com.mycompany.project.common.enums;

public enum EnrollmentStatus {
    APPLIED, // Enrollment request successful
    CANCELED, // Enrollment request canceled
    FORCED_CANCELED // Forced cancellation by admin
}
