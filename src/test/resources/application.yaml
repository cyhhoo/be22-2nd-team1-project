spring:
  datasource:
    url: spring.datasource.url=jdbc:mariadb://localhost:3306/test?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: org.mariadb.jdbc.Driver
    username: swcamp
    password: swcamp
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true



jwt:
  secret: testSecretKeyForTestingPurposesMustBeLongEnoughToSatisfyReviewRequirements2026!

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.mycompany.project
  configuration:
    map-underscore-to-camel-case: true