<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.project.attendance.mapper.AttendanceCodeQueryMapper">

    <!-- 출결 코드 단건 조회 -->
    <select id="findById" parameterType="long"
            resultType="com.mycompany.project.attendance.dto.response.AttendanceCodeResponse">
        SELECT
        attendance_code_id AS attendanceCodeId,
        code               AS code,
        name               AS name,
        is_excused         AS excused,
        is_active          AS active,
        created_at         AS createdAt,
        updated_at         AS updatedAt
        FROM tbl_attendance_code
        WHERE attendance_code_id = #{attendanceCodeId}
    </select>

    <!-- 출결 코드 전체 조회 -->
    <select id="findAll"
            resultType="com.mycompany.project.attendance.dto.response.AttendanceCodeResponse">
        SELECT
        attendance_code_id AS attendanceCodeId,
        code               AS code,
        name               AS name,
        is_excused         AS excused,
        is_active          AS active,
        created_at         AS createdAt,
        updated_at         AS updatedAt
        FROM tbl_attendance_code
        <where>
            <!-- activeOnly=true인 경우만 필터링 -->
            <if test="activeOnly != null and activeOnly == true">
                is_active = 1
            </if>
        </where>
        ORDER BY attendance_code_id ASC
    </select>

</mapper>
