server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0

# Disable Spring Boot 3.5.9 Compatibility Verifier
spring:
  cloud:
    compatibility-verifier:
      enabled: false
